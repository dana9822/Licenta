@model IEnumerable<GestiuneExamene.Models.Subject>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.Label("DenumireDisciplina", "Disciplina")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DenumireDisciplina)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdDisciplina }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdDisciplina }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdDisciplina })
        </td>
    </tr>
}

</table>
