@model GestiuneExamene.Models.SubjectCoverage

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>SubjectCoverage</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.IdSpecializare)

        @Html.HiddenFor(model => model.IdDisciplina)

        @Html.HiddenFor(model => model.MarcaProf)

        @Html.HiddenFor(model => model.AnStudiu)

        @Html.HiddenFor(model => model.AnUniversitar)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
