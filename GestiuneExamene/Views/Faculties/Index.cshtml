@{
    ViewBag.Title = "Faculties"; } <h2>@ViewBag.Title</h2>
    @foreach (var faculties in ViewBag.Faculties)
    {
        <div class="panel-body">
            @Html.Label("Name", "Denumire:")
            <br /> <p>@faculties.Title</p>
            @Html.Label("Adress", "Adresa:")
            <br />
            <p>@faculties.Author</p>
            @using (Html.BeginForm(actionName: "Detalii", controllerName: "Faculties", method: FormMethod.Get, routeValues: new { id = faculties.FacultyId }))
            {
                <button class="btn btn-primary">Details</button>
            }
        </div>
    }

    @Html.ActionLink("Add book", "New")