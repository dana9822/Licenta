@model IEnumerable<GestiuneExamene.Models.Building>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.Label("DenumireCorp", "Corp")
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Adresa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DenumireCorp)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Adresa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCorp }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCorp }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCorp })
        </td>
    </tr>
}

</table>
