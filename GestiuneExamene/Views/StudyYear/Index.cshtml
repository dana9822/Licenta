@model IEnumerable<GestiuneExamene.Models.StudyYear>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.Label("AnStudiu", "Anul de studiu")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AnStudiu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StudyYearId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StudyYearId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StudyYearId })
        </td>
    </tr>
}

</table>
